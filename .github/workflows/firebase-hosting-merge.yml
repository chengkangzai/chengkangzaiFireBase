# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - master

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Generate environment file
        run: |
          echo "export const environment = {" > chengkangzai/environment.prod.ts
          echo "  firebase: {" >> chengkangzai/environment.prod.ts
          echo "    projectId: '${{ secrets.FIREBASE_PROJECT_ID }}'," >> chengkangzai/environment.prod.ts
          echo "    appId: '${{ secrets.FIREBASE_APP_ID }}'," >> chengkangzai/environment.prod.ts
          echo "    databaseURL: '${{ secrets.FIREBASE_DATABASE_URL }}'," >> chengkangzai/environment.prod.ts
          echo "    storageBucket: '${{ secrets.FIREBASE_STORAGE_BUCKET }}'," >> chengkangzai/environment.prod.ts
          echo "    locationId: '${{ secrets.FIREBASE_LOCATION_ID }}'," >> chengkangzai/environment.prod.ts
          echo "    apiKey: '${{ secrets.FIREBASE_API_KEY }}'," >> chengkangzai/environment.prod.ts
          echo "    authDomain: '${{ secrets.FIREBASE_AUTH_DOMAIN }}'," >> chengkangzai/environment.prod.ts
          echo "    messagingSenderId: '${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}'," >> chengkangzai/environment.prod.ts
          echo "    measurementId: '${{ secrets.FIREBASE_MEASUREMENT_ID }}'," >> chengkangzai/environment.prod.ts
          echo "  }," >> chengkangzai/environment.prod.ts
          echo "  production: true" >> chengkangzai/environment.prod.ts
          echo "};" >> chengkangzai/environment.prod.ts
      - name: Install dependencies and build
        run: cd chengkangzai && yarn && yarn run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_CHENGKANGZAI }}'
          channelId: live
          projectId: chengkangzai
