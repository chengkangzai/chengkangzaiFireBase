# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - master

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Generate environment file
        run: |
          echo "export const environment = {" > chengkangzai/environment.ts
          echo "  firebase: {" >> chengkangzai/environment.ts
          echo "    projectId: '${{ secrets.FIREBASE_PROJECT_ID }}'," >> chengkangzai/environment.ts
          echo "    appId: '${{ secrets.FIREBASE_APP_ID }}'," >> chengkangzai/environment.ts
          echo "    databaseURL: '${{ secrets.FIREBASE_DATABASE_URL }}'," >> chengkangzai/environment.ts
          echo "    storageBucket: '${{ secrets.FIREBASE_STORAGE_BUCKET }}'," >> chengkangzai/environment.ts
          echo "    locationId: '${{ secrets.FIREBASE_LOCATION_ID }}'," >> chengkangzai/environment.ts
          echo "    apiKey: '${{ secrets.FIREBASE_API_KEY }}'," >> chengkangzai/environment.ts
          echo "    authDomain: '${{ secrets.FIREBASE_AUTH_DOMAIN }}'," >> chengkangzai/environment.ts
          echo "    messagingSenderId: '${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}'," >> chengkangzai/environment.ts
          echo "    measurementId: '${{ secrets.FIREBASE_MEASUREMENT_ID }}'," >> chengkangzai/environment.ts
          echo "  }," >> chengkangzai/environment.ts
          echo "  production: true" >> chengkangzai/environment.ts
          echo "};" >> chengkangzai/environment.ts
      - name: Install dependencies and build
        run: cd chengkangzai && yarn && yarn run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_CHENGKANGZAI }}'
          channelId: live
          projectId: chengkangzai
